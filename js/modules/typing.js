import{state,saveState,SENTENCES,getModeStats}from'./utils.js';import{updatePPMHistory,loadAchievements,checkRoundAchievements,trackSpaceKey,addGlobalXP}from'./stats.js';import{audioEngine}from'./audio.js';import{getModeHandler,getModeId,MODE_NAMES,renderModeDashboard}from'../modes/index.js';const hiddenInput=document.getElementById('hidden-input'),textDisplay=document.getElementById('text-display'),ppmVal=document.getElementById('ppm-val'),accuracyVal=document.getElementById('accuracy-val'),timerVal=document.getElementById('timer-val'),resultMessage=document.getElementById('result-message'),countdownTag=document.getElementById('countdown-tag'),progressFill=document.getElementById('progress-fill'),progressText=document.getElementById('progress-text'),progressPercent=document.getElementById('progress-percent'),modeStatusTag=document.getElementById('mode-status-tag');function syncRpgContinueButton(){const b=document.getElementById('rpg-continue-btn');if(!b)return;const r=getModeId()==='rpg';b.classList.toggle('hidden',!r);b.setAttribute('aria-hidden',String(!r))}document.addEventListener('modeEndTest',e=>{const{accuracy,wpm,modeId}=e.detail||{};if(modeId&&modeId!==getModeId())return;endTest(accuracy,wpm,modeId)});document.addEventListener('modeResetTest',()=>initTest());document.addEventListener('modeUpdateDisplay',e=>{if(e.detail?.html)textDisplay.innerHTML=e.detail.html});export function updateProgress(t,n){const p=n>0?Math.min(100,Math.round(t/n*100)):0;if(progressFill)progressFill.style.width=`${p}%`;if(progressText)progressText.textContent=`${t} / ${n} caracteres`;if(progressPercent)progressPercent.textContent=`${p}%`}function elapsed(){return state.startTime?Math.max(1,Math.floor((performance.now()-state.startTime)/1000)):0}function startTimer(){if(state.isRunning)return;state.isRunning=true;state.startTime=performance.now();const m=getModeHandler();if(m&&Object.prototype.hasOwnProperty.call(m,'startTime'))m.startTime=state.startTime;clearInterval(state.timerInterval);state.timerInterval=setInterval(()=>{const s=elapsed();if(timerVal&&!m?.hasTimer)timerVal.textContent=`${s}s`;const cm=getModeHandler();if(cm?.getMetrics){const x=cm.getMetrics();if(x?.wpm!==undefined){ppmVal.textContent=x.wpm;state.currentPPM=x.wpm}if(x?.accuracy!==undefined)accuracyVal.textContent=`${x.accuracy}%`;renderModeDashboard()}},1000)}export function endTest(a,w,id){if(state._ending)return;state._ending=true;clearInterval(state.timerInterval);clearInterval(state.timerModeInterval);state.timerInterval=null;state.timerModeInterval=null;state.isRunning=false;const m=getModeHandler(),mid=id||getModeId(),time=elapsed();if(timerVal&&!m?.hasTimer)timerVal.textContent=`${time}s`;const met=m?.getMetrics?.();a??=met?.accuracy??100;w??=met?.wpm??state.currentPPM??0;if(ppmVal)ppmVal.textContent=w;if(accuracyVal)accuracyVal.textContent=`${a}%`;let xp=Math.max(5,Math.round(w*2+a*.5)+(a===100?20:0));const chars=Math.max(0,Number(state.totalTyped)||0);addGlobalXP(xp,chars);updatePPMHistory(mid,w);const s=getModeStats(mid);s.rounds=(s.rounds||0)+1;s.totalTyped=(s.totalTyped||0)+chars;s.bestPPM=Math.max(s.bestPPM||0,Number(w)||0);s.bestAccuracy=Math.max(s.bestAccuracy||0,Number(a)||0);s.bestTime=s.bestTime==null?time:Math.min(s.bestTime,time);saveState();m?.checkMedals?.(a,w,time);const msg=m?.getResultMessage?.(a,w)||'';if(resultMessage){resultMessage.className='result-message success';resultMessage.innerHTML=`🎉 <strong>${a}%</strong> em ${time}s. ${msg}`;resultMessage.classList.remove('hidden')}checkRoundAchievements(w,a,state.currentTheme);loadAchievements();state._ending=false;if(hiddenInput)hiddenInput.disabled=true;syncRpgContinueButton()}export function initTest({resetMode=true}={}){syncRpgContinueButton();clearInterval(state.timerInterval);clearTimeout(state.autoRestartTimeout);state.timerInterval=null;state.isRunning=false;state.totalTyped=0;state.errors=0;state.startTime=null;state.previousInput='';state.currentPPM=0;state._ending=false;if(hiddenInput){hiddenInput.value='';hiddenInput.disabled=false}if(timerVal)timerVal.textContent='0s';if(ppmVal)ppmVal.textContent='0';if(accuracyVal)accuracyVal.textContent='100%';if(resultMessage){resultMessage.classList.add('hidden');resultMessage.innerHTML=''}clearInterval(state.timerModeInterval);state.timerModeInterval=null;if(countdownTag){countdownTag.classList.add('hidden');countdownTag.classList.remove('warning')}const m=getModeHandler();if(resetMode)m?.reset?.();const d=state.currentDifficulty;if(d==='custom'){if(!state.customUserText||state.customUserText.trim().length<10){textDisplay.innerHTML='<em>Nenhum texto customizado. Clique em "Alterar Texto".</em>';if(hiddenInput)hiddenInput.disabled=true;return}state.currentText=state.customUserText}else{const list=SENTENCES[d]||SENTENCES.easy;state.currentText=list[Math.floor(Math.random()*list.length)]}if(m?.init){m.init(state.currentText);if(Object.prototype.hasOwnProperty.call(m,'startTime'))m.startTime=null}else textDisplay.innerHTML=state.currentText.split('').map(ch=>`<span class="char">${ch}</span>`).join('');updateProgress(0,state.currentText.length);if(modeStatusTag)modeStatusTag.textContent=MODE_NAMES[getModeId()]||'Padrão';const s=getModeStats(getModeId()),b=document.getElementById('best-ppm-val');if(b)b.textContent=s.bestPPM||0;loadAchievements();renderModeDashboard()}function handleTyping(){const m=getModeHandler();if(!m||!hiddenInput)return;const v=hiddenInput.value;if(!state.isRunning&&v.length)startTimer();const r=m.handleInput(v);if(r){if(r.playError)audioEngine.playErrorSound();else if(r.playSound!==false)audioEngine.playKey(false);if(r.done){endTest(r.accuracy??100,r.wpm??state.currentPPM??0);return}if(r.reset){initTest();return}if(r.metrics){if(r.metrics.wpm!==undefined)ppmVal.textContent=r.metrics.wpm;if(r.metrics.accuracy!==undefined)accuracyVal.textContent=`${r.metrics.accuracy}%`;if(r.metrics.progress!==undefined)updateProgress(r.metrics.progress,state.currentText.length)}}renderModeDashboard()}export function setupTypingEvents(){if(!hiddenInput)return;hiddenInput.setAttribute('autocomplete','new-password');hiddenInput.setAttribute('autocorrect','off');hiddenInput.setAttribute('autocapitalize','none');hiddenInput.setAttribute('spellcheck','false');hiddenInput.setAttribute('data-form-type','other');hiddenInput.setAttribute('data-form-type','other');hiddenInput.setAttribute('spellcheck','false');hiddenInput.addEventListener('keydown',e=>{if(e.key===' ')trackSpaceKey()});hiddenInput.addEventListener('beforeinput',e=>{if(/insertFromPaste|insertFromDrop|insertReplacementText|insertFromYank/.test(e.inputType)){e.preventDefault();return}if(e.data&&e.data.length>1){e.preventDefault();return}if((e.inputType==='insertText'||e.inputType==='insertCompositionText')&&e.data)state.totalTyped+=e.data.length});hiddenInput.addEventListener('paste',e=>e.preventDefault());hiddenInput.addEventListener('contextmenu',e=>e.preventDefault());hiddenInput.addEventListener('input',handleTyping);if(textDisplay)textDisplay.addEventListener('click',()=>{if(!hiddenInput.disabled)hiddenInput.focus({preventScroll:true})})}